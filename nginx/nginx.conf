# If we receive X-Forwarded-Proto, pass it through; otherwise, pass along the
# scheme used to connect to this server
map $http_x_forwarded_proto $proxy_x_forwarded_proto {
  default $http_x_forwarded_proto;
  ''      $scheme;
}
# If we receive X-Forwarded-Port, pass it through; otherwise, pass along the
# server port the client connected to
map $http_x_forwarded_port $proxy_x_forwarded_port {
  default $http_x_forwarded_port;
  ''      $server_port;
}
# If we receive Upgrade, set Connection to "upgrade"; otherwise, delete any
# Connection header that may have been passed to this server
map $http_upgrade $proxy_connection {
  default upgrade;
  '' close;
}
# Apply fix for very long server names
server_names_hash_bucket_size 128;
# Default dhparam
ssl_dhparam /etc/nginx/certs/dhparam.pem;
# Set appropriate X-Forwarded-Ssl header
map $scheme $proxy_x_forwarded_ssl {
  default off;
  https on;
}

gzip_types text/plain text/css application/javascript application/json application/x-javascript text/xml application/xml application/xml+rss text/javascript;
log_format vhost '$host $remote_addr - $remote_user [$time_local] '
                 '"$request" $status $body_bytes_sent '
                 '"$http_referer" "$http_user_agent"';
access_log off;


# HTTP 1.1 support
proxy_http_version 1.1;
proxy_buffering off;
proxy_set_header Host $http_host;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection $proxy_connection;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $proxy_x_forwarded_proto;
proxy_set_header X-Forwarded-Ssl $proxy_x_forwarded_ssl;
proxy_set_header X-Forwarded-Port $proxy_x_forwarded_port;

ssl_session_timeout 5m;
ssl_session_cache shared:SSL:50m;
ssl_session_tickets off;
ssl_certificate /etc/nginx/certs/vis.crt;
ssl_certificate_key /etc/nginx/certs/vis.key;
add_header Strict-Transport-Security "max-age=31536000" always;


upstream awx.atvts2820.athtem.eei.ericsson.se {
    server awx_web:8052;
}

upstream grafana.atvts2820.athtem.eei.ericsson.se {
    server visualisationdocker_grafana_1:3000;
}

upstream prometheus.atvts2820.athtem.eei.ericsson.se {
    server visualisationdocker_prometheus_1:9090;
}

server {
        server_name awx.atvts2820.athtem.eei.ericsson.se;
        listen 80 ;
        access_log /var/log/nginx/access.log vhost;
        location / {
                return 301 https://$host$request_uri;
        }
}
server {
        server_name awx.atvts2820.athtem.eei.ericsson.se;
        listen 443 ssl http2 ;
        access_log /var/log/nginx/access.log vhost;
        location / {
                proxy_pass http://awx.atvts2820.athtem.eei.ericsson.se;
        }
}


server {
        server_name grafana.atvts2820.athtem.eei.ericsson.se;
        listen 80 ;
        access_log /var/log/nginx/access.log vhost;
        location / {
                return 301 https://$host$request_uri;
        }
}
server {
        server_name grafana.atvts2820.athtem.eei.ericsson.se;
        listen 443 ssl http2 ;
        access_log /var/log/nginx/access.log vhost;
        location / {
                proxy_pass http://grafana.atvts2820.athtem.eei.ericsson.se;
        }
}


server {
        server_name prometheus.atvts2820.athtem.eei.ericsson.se;
        listen 80 ;
        access_log /var/log/nginx/access.log vhost;
        location / {
                return 301 https://$host$request_uri;
        }
}
server {
        server_name prometheus.atvts2820.athtem.eei.ericsson.se;
        listen 443 ssl http2 ;
        access_log /var/log/nginx/access.log vhost;
        location / {
                proxy_pass http://prometheus.atvts2820.athtem.eei.ericsson.se;
        }
}
